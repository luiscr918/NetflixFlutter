import 'package:flutter/material.dart';

class ReproduccionScreen extends StatelessWidget {
  const ReproduccionScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Pantalla Bienvenida"),
      ),
    );
  }
}