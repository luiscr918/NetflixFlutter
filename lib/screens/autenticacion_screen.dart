import 'package:flutter/material.dart';

class AutenticacionScreen extends StatelessWidget {
  const AutenticacionScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Pantalla Autenticacion"),
      ),
    );
  }
}